# Архитектура приложения

## Компоненты

### App.kt
Главный компонент приложения с Material Theme и нижней навигацией:
- Scaffold с NavigationBar
- Переключение между экранами Чат и Настройки

### ChatScreen.kt
Экран чата с основным функционалом:
- LazyColumn для списка сообщений (reverseLayout для отображения новых снизу)
- Автоматическая подгрузка при прокрутке к началу списка
- Визуальное разделение сообщений:
  - Мои сообщения (от текущего автора) выровнены по правому краю с отступом 15% слева
  - Чужие сообщения занимают всю ширину
  - Одинаковый цвет карточек для всех сообщений
- Форма ввода:
  - Поле "Автор" (однострочное, не расширяется по вертикали)
  - Поле "Текст" (многострочное, 2-6 строк)
  - Кнопка отправки прямоугольная со скруглениями (64x56dp) с иконкой (➤ для отправки, 💾 для сохранения)
  - Кнопка отмены (64x40dp) при редактировании
- Компактный layout: кнопка отправки в одном ряду с полем текста

### ChatViewModel.kt
Управление состоянием чата:
- StateFlow для messages и isLoading
- Метод loadMoreMessages() для пагинации
- Метод sendMessage() для отправки новых сообщений
- Использует CoroutineScope для асинхронных операций

### MockBackend.kt
Заглушка бекенда:
- Хранит список сообщений в памяти
- Генерирует тестовые данные при инициализации
- Поддерживает пагинацию (по 10 сообщений)
- Имитирует задержку сети (500ms)

### Message.kt
Модель данных:
```kotlin
data class Message(
    val id: Int,
    val author: String,
    val text: String,
    val timestamp: String
)
```

## Поток данных

1. ChatViewModel инициализируется и загружает первую страницу сообщений
2. ChatScreen подписывается на StateFlow и отображает сообщения
3. При прокрутке к началу списка вызывается loadMoreMessages()
4. При отправке сообщения вызывается sendMessage(), новое сообщение добавляется в начало списка
5. При редактировании вызывается editMessage(), сообщение обновляется в списке
6. UI автоматически обновляется через StateFlow

## Функции сообщений

### Кебаб-меню
Каждое сообщение имеет меню с тремя действиями:

1. **Ответить** - всегда доступно, цитирует все сообщение
2. **Ответить с цитатой** - цитирует выделенный текст (если текст выделен)
3. **Редактировать** - доступно только для сообщений текущего пользователя

### Цитирование
- Цитата отображается над формой ввода в отдельной карточке
- Можно удалить цитату до отправки
- Цитата добавляется в начало сообщения с префиксом ">"

### Редактирование
- При редактировании текст сообщения загружается в форму
- Отображается индикатор "Редактирование сообщения"
- Кнопка "Отправить" меняется на "Сохранить"
- Доступна кнопка "Отмена" для выхода из режима редактирования

## Темы

Приложение поддерживает темную и светлую темы:

- **По умолчанию**: темная тема
- **Переключение**: через настройки (вкладка "Настройки")
- **Сохранение**: выбор темы сохраняется в localStorage
- **Реализация**: использует Material Design 3 colorScheme (darkColorScheme/lightColorScheme)

## Интеграция с реальным API

Для подключения к реальному бекенду:

1. Добавьте Ktor Client в зависимости:
```kotlin
implementation("io.ktor:ktor-client-core:2.3.7")
implementation("io.ktor:ktor-client-js:2.3.7")
implementation("io.ktor:ktor-client-content-negotiation:2.3.7")
implementation("io.ktor:ktor-serialization-kotlinx-json:2.3.7")
```

2. Замените MockBackend на реальный API клиент
3. Добавьте обработку ошибок и состояния загрузки
