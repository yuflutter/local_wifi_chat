# 🎙️ Audio Room Feature

Клиентский функционал аудио-комнаты на базе WebSocket с архитектурой MCU без микширования на сервере.

## 📋 Описание

Реализация аудио-комнаты для Flutter Web приложения с следующими возможностями:

- ✅ WebSocket соединение с сервером
- ✅ Получение списка участников и их статусов
- ✅ Воспроизведение аудио-потоков от других участников
- ✅ Регулировка громкости каждого участника
- ✅ Отправка аудио с микрофона на сервер
- ✅ Обновление мета-информации (статус микрофона)
- ✅ Микширование аудио в браузере (Web Audio API)
- ✅ Заглушка бекенда на Go

## 🏗️ Архитектура

### MCU без микширования

Сервер работает как центральный узел (MCU), но **не выполняет микширование**:
- Каждый клиент отправляет свой аудио-поток на сервер
- Сервер пересылает потоки всем участникам (кроме источника)
- Микширование происходит в браузере каждого клиента
- Каждый клиент может независимо регулировать громкость участников

### Структура проекта

```
lib/features/voice_room/
├── models/
│   ├── participant.dart          # Модель участника
│   └── ws_message.dart           # Модель WebSocket сообщений
├── services/
│   ├── audio_room_service.dart   # WebSocket соединение
│   ├── audio_player_service.dart # Воспроизведение аудио
│   └── audio_recorder_service.dart # Запись с микрофона
├── providers/
│   └── audio_room_provider.dart  # Управление состоянием
├── widgets/
│   ├── connection_dialog.dart    # Диалог подключения
│   └── participant_tile.dart     # Карточка участника
└── view/
    └── audio_page.dart           # Главная страница

backend_mock/
├── main.go                       # WebSocket сервер
├── go.mod                        # Go зависимости
├── README.md                     # Документация бекенда
├── API.md                        # Описание API
└── test_client.html              # Тестовый HTML клиент
```

## 🚀 Быстрый старт

### Автоматический запуск

```bash
./start_audio_room.sh
```

### Ручной запуск

1. **Установите зависимости Flutter:**
```bash
flutter pub get
```

2. **Запустите бекенд:**
```bash
cd backend_mock
go mod download
go run main.go
```

3. **Запустите Flutter приложение:**
```bash
flutter run -d chrome --web-browser-flag "--disable-web-security"
```

4. **Подключитесь к комнате:**
   - Откройте вкладку "Аудио"
   - Введите имя и подключитесь к `ws://localhost:8080/audio`
   - Нажмите на кнопку микрофона для включения

## 📡 Протокол WebSocket

### Подключение

```json
{
  "type": "metadata",
  "data": {
    "action": "join",
    "userId": "unique-id",
    "userName": "User Name"
  }
}
```

### Список участников (от сервера)

```json
{
  "type": "metadata",
  "data": {
    "participants": [
      {
        "id": "user-id",
        "name": "User Name",
        "isMuted": false,
        "isSpeaking": false,
        "volume": 1.0
      }
    ]
  }
}
```

### Обновление статуса

```json
{
  "type": "participant_update",
  "data": {
    "userId": "user-id",
    "isMuted": true
  }
}
```

### Аудио данные (Binary)

Формат: `[36 байт ID участника][аудио данные WebM/Opus]`

## 🎯 Основные компоненты

### AudioRoomService

Управляет WebSocket соединением:
- Подключение/отключение
- Отправка/получение сообщений
- Обработка аудио-чанков
- Управление списком участников

### AudioPlayerService

Воспроизведение аудио:
- Web Audio API для микширования
- Независимая регулировка громкости
- Управление аудио-контекстами для каждого участника

### AudioRecorderService

Запись с микрофона:
- MediaRecorder API
- Формат WebM/Opus
- Чанки по 100ms для низкой задержки
- Обработка разрешений

### AudioRoomProvider

Управление состоянием:
- Координация всех сервисов
- Реактивное обновление UI
- Обработка ошибок

## 🧪 Тестирование

### С Flutter приложением

1. Откройте несколько вкладок браузера
2. Подключитесь с разными именами
3. Включите микрофон в одной вкладке
4. Проверьте воспроизведение в других

### С HTML тестовым клиентом

```bash
cd backend_mock
# Откройте test_client.html в браузере
```

## 📚 Документация

- `AUDIO_ROOM_SETUP.md` - Подробная инструкция по настройке
- `backend_mock/README.md` - Документация бекенда
- `backend_mock/API.md` - Описание WebSocket API

## 🔧 Технологии

### Frontend (Flutter Web)
- `web_socket_channel` - WebSocket клиент
- `record` - Запись аудио
- `provider` - Управление состоянием
- Web Audio API - Воспроизведение и микширование

### Backend (Go)
- `gorilla/websocket` - WebSocket сервер
- Стандартная библиотека Go

## ⚙️ Конфигурация

### Формат аудио

- **Кодек**: Opus
- **Контейнер**: WebM
- **Частота чанков**: 100ms
- **Настройки**: Echo cancellation, Noise suppression, Auto gain control

### WebSocket

- **URL по умолчанию**: `ws://localhost:8080/audio`
- **Протокол**: Binary + JSON
- **Reconnection**: Ручное (в будущем - автоматическое)

## 🎨 UI Features

- Список участников с аватарами
- Индикатор статуса микрофона
- Слайдеры громкости для каждого участника
- Кнопка mute для быстрого отключения
- Индикатор состояния подключения
- Большая кнопка управления микрофоном

## 🔐 Безопасность

⚠️ **Текущая реализация - для разработки!**

Для продакшена необходимо:
- [ ] Использовать WSS (WebSocket Secure)
- [ ] Добавить аутентификацию
- [ ] Добавить авторизацию
- [ ] Валидация данных на сервере
- [ ] Rate limiting
- [ ] CORS настройки

## 🚧 Известные ограничения

1. Нет автоматического переподключения
2. Нет детекции активности голоса (VAD)
3. Нет поддержки комнат (rooms)
4. Нет записи сессий
5. Нет статистики качества соединения

## 🔮 Будущие улучшения

- [ ] Автоматическое переподключение
- [ ] Voice Activity Detection (VAD)
- [ ] Множественные комнаты
- [ ] Администрирование (kick, mute)
- [ ] Запись сессий
- [ ] Статистика (latency, packet loss)
- [ ] Адаптивный битрейт
- [ ] Поддержка мобильных платформ

## 📝 Лицензия

Часть проекта Local WiFi Chat

## 👥 Авторы

Разработано для проекта Local WiFi Chat Frontend
